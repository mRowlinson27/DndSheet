<UserControl x:Class="WpfUI.Controls.DictionaryTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">


    <DataGrid  x:Name="DataGrid" AutoGenerateColumns="True" 
                   ItemsSource="{Binding DataGridBinding}"
                   CellEditEnding="DataGrid_OnCellEditEnding"
                   IsReadOnly="True"
                   IsEnabled="True"
                   CanUserAddRows="false" ColumnWidth="*"
                   ColumnHeaderHeight="25">
        <DataGrid.Resources>
            <Style TargetType="{x:Type DataGridCell}">
                <Style.Triggers>
                    <Trigger Property="IsReadOnly" Value="False">
                        <Setter Property="Background" Value="PaleGoldenrod"></Setter>
                    </Trigger>
                    <Trigger Property="DataGridCell.IsSelected" Value="True">
                        <Setter Property="Background" Value="LightGray" />
                        <Setter Property="Foreground" Value="Black" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.Resources>
    </DataGrid>

</UserControl>
